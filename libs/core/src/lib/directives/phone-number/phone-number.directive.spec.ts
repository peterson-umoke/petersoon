import { PhoneNumberDirective } from './phone-number.directive';

describe('PhoneNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneNumberDirective(null);
    expect(directive).toBeTruthy();
  });
});
