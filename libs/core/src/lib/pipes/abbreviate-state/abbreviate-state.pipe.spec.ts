import { AbbreviateStatePipe } from './abbreviate-state.pipe';

describe('AbbreviateStatePipe', () => {
  it('create an instance', () => {
    const pipe = new AbbreviateStatePipe();
    expect(pipe).toBeTruthy();
  });
});
