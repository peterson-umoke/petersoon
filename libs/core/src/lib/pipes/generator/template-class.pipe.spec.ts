import { TemplateClassPipe } from './template-class.pipe';

describe('TemplateClassPipe', () => {
  it('create an instance', () => {
    const pipe = new TemplateClassPipe();
    expect(pipe).toBeTruthy();
  });
});
